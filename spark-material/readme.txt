1. To setup spark, just run set setup.sh executable. This will setup spark and set environment variables for bash.(you need to change shell to bash).

2. The guide to create and run spark programs using maven is given in the maven_run_spark.txt file, an example spark program called sparkjob is given for reference
